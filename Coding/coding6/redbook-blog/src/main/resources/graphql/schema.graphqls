type Query{
    postById(id: ID): Post
}

type Mutation{
    createPost(title: String!, description: String!, content: String!): Post
}

type Post{
    id:ID
    title:String
    description: String
    content: String
}